//en realidad, todo puede hacerse con constructor... pero no siempre vale la pena!!

const num = new Number(16);

console.log(num);
console.log(typeof num); //miren qué curioso

console.log(num + 8); //sin embargo, ya una vez creado lo podemos usar

//todo objecto tiene un constructor. La función constructor es lo que se va a usar para crearlo.

